import { ValidCreateFormDirective } from './valid-create-form.directive';

describe('ValidCreateFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidCreateFormDirective();
    expect(directive).toBeTruthy();
  });
});
